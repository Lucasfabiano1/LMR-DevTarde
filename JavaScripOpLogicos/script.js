function nParImpar() {
    let numero = document.getElementById("nDigitado").value;
    let verificar = parseInt(numero) % 2;
    if (verificar == o) {
        alert("o n° " + numero + " é par")
    } else {
        alert("o n° " + numero + " é impar sim")
    }

}